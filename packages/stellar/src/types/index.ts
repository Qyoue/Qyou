/**
 * Placeholder type namespace for the future Stellar integration package.
 *
 * No Stellar SDK types or blockchain-specific shapes are defined yet. This
 * file exists so the package compiles and so future modules have a known
 * location to add Stellar-related types.
 */
export type StellarPackagePlaceholder = Record<string, never>;
